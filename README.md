# Discord Lite

## Start

Run both server and client:

```powershell
.\run-discord-lite.cmd
```

Start only the server:

```powershell
.\run-server.cmd
```

Start only the client:

```powershell
.\run-client.cmd
```

## Requirement

Make sure Java 25 is installed and `java -version` works in a terminal.
